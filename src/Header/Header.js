import React from 'react';

const Header = () => {
    return (
        <header>
            <button>HOME</button>
            <button>SEARCH</button>

            <h1>PROFILE</h1>
        </header>
    )
}

export default Header;