import React from 'react';
import './Header.css';

const Header = () => {
    return (
        <header>
            <h4>TOMATILLOS</h4>
            <button>HOME</button>

            <h4>PROFILE</h4>
            <button>SEARCH</button>
        </header>
    )
}

export default Header;